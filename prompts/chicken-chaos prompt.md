# Chicken Chaos - build prompt

<!-- GENERATED by _gen_game_prompts.py from FuturePlans/GAME_IDEAS.md. Do not hand-edit; edit the data
     in that script and re-run it. The living status of this game lives in FuturePlans/GAME_IDEAS.md. -->

| | |
|---|---|
| RS id | `RS-015` |
| Manifest id | `chicken-chaos` |
| Status | SHIPPED (v4) and LOCKED |
| Type | Arena |
| Players | 2-4 + bots |
| Tier | Tier 1 |
| Orientation | landscape |
| Target file | `app/src/main/assets/games/chicken-chaos/index.html` |
| Manifest values | `id chicken-chaos / landscape / minPlayers 2 / maxPlayers 4 / aiSupport true / online false / tileColor #6BCB4A / version 4` |

## THE IDEA (one paragraph, no more)

Grab the chicken, dash away, do not get tackled: a same-screen scramble where everyone fights over one bird. The game ships today, is locked, and needs nothing - this prompt exists as the reference for what a finished landscape arena game looks like in this shell.

## THIS GAME'S SPECIFICS

- LOCKED: do not start work on this game unless the director explicitly reopens it. Its contract is complete: manifest v4 with a changelog block, the Studio SDK, the full `window.Game` lifecycle, settings sync (sound / haptics / volume via `Game.setSettings`), gesture-gated audio unlock, and the landscape turn owned by the shell (no WebView fullscreen call).
- Shipped rules: SCORE = chickens caught. No target score and no early win path; a match always runs the full 60 s and the player with the most catches wins (ties are called as ties).
- Shipped presentation, so it is not accidentally undone: in-game texts are viewport-scaled captions, back/gear use the game's own unselected-toggle face, the in-game pause keeps its original dark translucent disc, catch boxes stay smaller than the pause button, and the result card is a cream board with a grass paddock + the start-page chicken on the left and the win/tie headline (TIME UP! styling) plus the catch table on the right.
- Ads: the interstitial runs when the player taps PLAY AGAIN or MENU on the result card - never over the result itself.
- Full history: FuturePlans/GAME_IDEAS.md section 6.13 and the manifest changelog inside the file.

## HARD REQUIREMENTS (our shell stack - all mandatory)

1. ONE self-contained `.html`. All CSS, JS and art inline.
2. ZERO network calls at runtime: no CDNs, no external fonts, no analytics, no remote images.
   It must run perfectly from `file://` with the device offline.
3. Size budget: Tier 1 - see `CONTRACT.md` §2 (T1 <= 300 KB, T2 <= 450 KB, T3 <= 1 MB).
4. `STUDIO_GAME_MANIFEST` comment block with exactly these keys -
   `id / title / orientation / minPlayers / maxPlayers / aiSupport / online / tileColor / version`
   - values: `id chicken-chaos / landscape / minPlayers 2 / maxPlayers 4 / aiSupport true / online false / tileColor #6BCB4A / version 4`.
5. `window.Game` lifecycle with a simulation clock: `pause()`, `resume()`, `destroy()`.
   Pausing must freeze all timers, animation and audio; nothing may keep simulating in the
   background.
6. Studio SDK block, wrapped so the game runs fully when the native bridge is absent
   (browser test).
7. Ads ONLY through the shell bridge (banner + rewarded); a rewarded callback that arrives
   with no argument means GRANTED. If the device is offline, show a plain "check your
   internet connection" message - NEVER a simulated or fake ad.
8. Audio and vibration obey the shell settings (master sound, volume, vibration, haptic
   profile): no game sound may play when master sound is off. `Game.setSettings` receives
   `sound` / `haptics` / `volume` from the shell.
9. User-facing copy: plain text, no em dashes and no "--" anywhere in the UI.
10. Orientation landscape, with safe-area insets handled (notches, rounded corners).
11. Every inline `<script>` block must parse - verified with the syntax gate below.

## DO NOT

- Change the shell, add a second file, add a dependency, phone home, or invent gameplay
  beyond the idea above.
- Touch any file other than the target file (and, when the entry says so, the manifest
  block inside it).
- Launch the app or the game yourself. Build and install only; the director tests on device.

## DONE MEANS

- `node Tools/_check_js_syntax.js app/src/main/assets/games/chicken-chaos/index.html` -> `ALL SCRIPTS PARSE OK`.
- `BUILD.bat` -> `BUILD OK - shell-debug.apk`, and only then `adb install -r shell-debug.apk`.
- Nothing to do. If reopened: the syntax gate passes, the build installs, and only the director's named items change.
- `FuturePlans/GAME_IDEAS.md` status updated (and `PROGRESS.md` current state if the game shipped).

## OPEN QUESTIONS FOR THE DIRECTOR

- Only reopen this game with a specific director-requested change.
