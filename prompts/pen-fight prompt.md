# Pen Fight - build prompt

<!-- GENERATED by _gen_game_prompts.py from FuturePlans/GAME_IDEAS.md. Do not hand-edit; edit the data
     in that script and re-run it. The living status of this game lives in FuturePlans/GAME_IDEAS.md. -->

| | |
|---|---|
| RS id | `RS-012` |
| Manifest id | `pen-fight` |
| Status | PLAYABLE - has director-reported issues |
| Type | Arena |
| Players | 2-5 + AI |
| Tier | Tier 2 (physics) |
| Orientation | landscape |
| Target file | `port source: sources/Playable Games/PenFight.html -> app/src/main/assets/games/pen-fight/index.html` |
| Manifest values | `proposed: id pen-fight / landscape / minPlayers 2 / maxPlayers 5 / aiSupport true / online false / tileColor ink blue / version 1` |

## THE IDEA (one paragraph, no more)

Desk-warfare nostalgia: pull back, release, flick your rival's pen off the bench. Real-time physics, up to five players sharing one screen.

## THIS GAME'S SPECIFICS

- CONTRACT VIOLATION TO FIX FIRST: the file loads Matter.js from a CDN, so the game fails offline ("Couldn't load the physics engine"). Inline matter.js (minified ~150 KB, total ~230 KB, inside the Tier-2 budget) or replace it with our own tiny circle-vs-circle collision code.
- MISSING: the manifest block, and a real lifecycle - `pause` / `destroy` and sim-clock freezing are not implemented, so a backgrounded game keeps simulating.
- AI quality is the weakest area: heuristic aim at the nearest enemy plus a power search over a small candidate set, then tune.
- Physics must feel repeatable and stay time-based, never frame-based (frame rate changes flick distance). The layout must let five players each reach a control on one screen.
- ASK THE DIRECTOR which "serious issues" were meant. Provable from the code: the CDN dependency, the missing manifest/lifecycle, AI difficulty. Full entry: FuturePlans/GAME_IDEAS.md section 6.10.

## HARD REQUIREMENTS (our shell stack - all mandatory)

1. ONE self-contained `.html`. All CSS, JS and art inline.
2. ZERO network calls at runtime: no CDNs, no external fonts, no analytics, no remote images.
   It must run perfectly from `file://` with the device offline.
3. Size budget: Tier 2 (physics) - see `CONTRACT.md` §2 (T1 <= 300 KB, T2 <= 450 KB, T3 <= 1 MB).
4. `STUDIO_GAME_MANIFEST` comment block with exactly these keys -
   `id / title / orientation / minPlayers / maxPlayers / aiSupport / online / tileColor / version`
   - values: `proposed: id pen-fight / landscape / minPlayers 2 / maxPlayers 5 / aiSupport true / online false / tileColor ink blue / version 1`.
5. `window.Game` lifecycle with a simulation clock: `pause()`, `resume()`, `destroy()`.
   Pausing must freeze all timers, animation and audio; nothing may keep simulating in the
   background.
6. Studio SDK block, wrapped so the game runs fully when the native bridge is absent
   (browser test).
7. Ads ONLY through the shell bridge (banner + rewarded); a rewarded callback that arrives
   with no argument means GRANTED. If the device is offline, show a plain "check your
   internet connection" message - NEVER a simulated or fake ad.
8. Audio and vibration obey the shell settings (master sound, volume, vibration, haptic
   profile): no game sound may play when master sound is off. `Game.setSettings` receives
   `sound` / `haptics` / `volume` from the shell.
9. User-facing copy: plain text, no em dashes and no "--" anywhere in the UI.
10. Orientation landscape, with safe-area insets handled (notches, rounded corners).
11. Every inline `<script>` block must parse - verified with the syntax gate below.

## DO NOT

- Change the shell, add a second file, add a dependency, phone home, or invent gameplay
  beyond the idea above.
- Touch any file other than the target file (and, when the entry says so, the manifest
  block inside it).
- Launch the app or the game yourself. Build and install only; the director tests on device.

## DONE MEANS

- `node Tools/_check_js_syntax.js port source: sources/Playable Games/PenFight.html -> app/src/main/assets/games/pen-fight/index.html` -> `ALL SCRIPTS PARSE OK`.
- `BUILD.bat` -> `BUILD OK - shell-debug.apk`, and only then `adb install -r shell-debug.apk`.
- Runs fully offline from file://, manifest + lifecycle in place, a full 2-5 player round played on device with the AI feeling fair.
- `FuturePlans/GAME_IDEAS.md` status updated (and `PROGRESS.md` current state if the game shipped).

## OPEN QUESTIONS FOR THE DIRECTOR

- Which issues did you mean by "serious issues"? They go to the top of this prompt.
- Inline matter.js, or write our own tiny collision code?
