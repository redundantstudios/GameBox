# Party Pack (shell section + the small party games) - build prompt

<!-- GENERATED by _gen_game_prompts.py from FuturePlans/GAME_IDEAS.md. Do not hand-edit; edit the data
     in that script and re-run it. The living status of this game lives in FuturePlans/GAME_IDEAS.md. -->

| | |
|---|---|
| RS id | `RS-008` |
| Manifest id | `party-pack` |
| Status | IDEA |
| Type | Party |
| Players | group on one phone |
| Tier | Tier 1 |
| Orientation | portrait |
| Target file | `pilot: new file app/src/main/assets/games/party-clue-and-guess/index.html (+ a PARTY entry point in the shell)` |
| Manifest values | `proposed pilot: id party-clue-and-guess / portrait / minPlayers 3 / maxPlayers 8 / aiSupport none / online false / tileColor party-pink / version 1` |

## THE IDEA (one paragraph, no more)

A pack of shout-out-loud party games for one phone passed around a room: clue-and-guess, dumb charades and truth-or-dare, each turn short and loud enough to be funny from across a table.

## THIS GAME'S SPECIFICS

- Three launch modules: 1) CLUE & GUESS - one player sees a word and gives clues while the group guesses against a timer (the pilot). 2) DUMB CHARADES - the phone shows a word to the actor only, with categories and difficulty tiers. 3) TRUTH OR DARE - a local deck, clean/classic by default, with an explicit age toggle.
- SHELL WORK THIS IMPLIES (not built yet): a manifest `category` field (`board|classic|party|arena|puzzle`) parsed next to the others, plus a PARTY entry point on Home (the way ALL GAMES works) or a filter on the Mode screen. Reuse the existing pass-and-flip mechanics instead of inventing them.
- Policy: local-only (no network, so no online-interaction declaration). Ads: banner on results only. Truth-or-dare content must be tame by default; age rating and content review are real tasks, not line items.
- Group-sized type: one small screen read at arm's length. Full entry: FuturePlans/GAME_IDEAS.md section 6.6.

## HARD REQUIREMENTS (our shell stack - all mandatory)

1. ONE self-contained `.html`. All CSS, JS and art inline.
2. ZERO network calls at runtime: no CDNs, no external fonts, no analytics, no remote images.
   It must run perfectly from `file://` with the device offline.
3. Size budget: Tier 1 - see `CONTRACT.md` §2 (T1 <= 300 KB, T2 <= 450 KB, T3 <= 1 MB).
4. `STUDIO_GAME_MANIFEST` comment block with exactly these keys -
   `id / title / orientation / minPlayers / maxPlayers / aiSupport / online / tileColor / version`
   - values: `proposed pilot: id party-clue-and-guess / portrait / minPlayers 3 / maxPlayers 8 / aiSupport none / online false / tileColor party-pink / version 1`.
5. `window.Game` lifecycle with a simulation clock: `pause()`, `resume()`, `destroy()`.
   Pausing must freeze all timers, animation and audio; nothing may keep simulating in the
   background.
6. Studio SDK block, wrapped so the game runs fully when the native bridge is absent
   (browser test).
7. Ads ONLY through the shell bridge (banner + rewarded); a rewarded callback that arrives
   with no argument means GRANTED. If the device is offline, show a plain "check your
   internet connection" message - NEVER a simulated or fake ad.
8. Audio and vibration obey the shell settings (master sound, volume, vibration, haptic
   profile): no game sound may play when master sound is off. `Game.setSettings` receives
   `sound` / `haptics` / `volume` from the shell.
9. User-facing copy: plain text, no em dashes and no "--" anywhere in the UI.
10. Orientation portrait, with safe-area insets handled (notches, rounded corners).
11. Every inline `<script>` block must parse - verified with the syntax gate below.

## DO NOT

- Change the shell, add a second file, add a dependency, phone home, or invent gameplay
  beyond the idea above.
- Touch any file other than the target file (and, when the entry says so, the manifest
  block inside it).
- Launch the app or the game yourself. Build and install only; the director tests on device.

## DONE MEANS

- `node Tools/_check_js_syntax.js pilot: new file app/src/main/assets/games/party-clue-and-guess/index.html (+ a PARTY entry point in the shell)` -> `ALL SCRIPTS PARSE OK`.
- `BUILD.bat` -> `BUILD OK - shell-debug.apk`, and only then `adb install -r shell-debug.apk`.
- The pilot module (Clue & Guess) playable end-to-end on device, the `category` field parsed by the shell, and a PARTY entry point visible on Home.
- `FuturePlans/GAME_IDEAS.md` status updated (and `PROGRESS.md` current state if the game shipped).

## OPEN QUESTIONS FOR THE DIRECTOR

- Agree the three launch modules.
- Who writes the content deck?
- A PARTY band on Home, or a filter on the Mode screen?
